{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd skyright-forecaster/skyright-forecaster && npm install --include=dev && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd skyright-forecaster/skyright-forecaster && npm install --include=dev && npm run build && node dist/index.js",
    "restartPolicyMaxRetries": 5
  }
}
